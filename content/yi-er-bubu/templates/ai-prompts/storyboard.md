# 任务：逐页双格分镜

根据下方已审核脚本和角色基准，按照 `templates/storyboard.md` 的字段生成完整分镜。

要求：

- 页面为 1080×1440 白色外画布，上格 `x=64..1016, y=72..654`，下格 `x=64..1016, y=726..1308`。
- 逐格写清场景、景别、角色位置、动作、表情和连续道具。
- 每句对白记录“说话角色 + 角色所在方位 + 气泡尾尖坐标”；角色名只作内部配对。
- 给每格保留气泡安全区，尾尖不得指向另一个角色或空处。
- 画面本身不得包含文字、数字、气泡、签名、水印或模型自画边框。

只输出可直接保存为 `storyboard.md` 的 Markdown。

## 已审核输入

<依次粘贴 script.md 与 character-bible.md；提交调用前删除本行占位说明>
